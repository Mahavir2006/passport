# Supabase — Vite exposes only VITE_* vars to the browser bundle
VITE_SUPABASE_URL=https://*******.supabase.co
VITE_SUPABASE_ANON_KEY=*************

# Server-side only — never import these in client code
SUPABASE_SERVICE_ROLE_KEY=***************************888
