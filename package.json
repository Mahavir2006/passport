{
  "name": "agentpassport",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n server,mock,client -c blue,magenta,green \"npm run dev --prefix server\" \"node server/mock-agent/server.js\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
